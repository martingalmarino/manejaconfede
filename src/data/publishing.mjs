/** Shared publishing controls for Astro config and site runtime. */
export const publishingMode = /** @type {'preview' | 'production'} */ ('preview');

/** Real domain without protocol, e.g. "manejaconfede.com". Leave empty until confirmed. */
export const canonicalDomain = '';
